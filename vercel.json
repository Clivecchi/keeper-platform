{
  "buildCommand": "pnpm install && pnpm add -D prisma && pnpm prisma generate && pnpm run build",
  "installCommand": "pnpm install",
  "framework": "vite",
  "outputDirectory": "dist",
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/node"
    }
  ]
} 