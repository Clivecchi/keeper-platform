{
  "build": {
    "env": {
      "NODE_ENV": "production"
    },
    "command": "pnpm install && pnpm --filter @keeper/database generate && pnpm turbo build --filter=apps/api..."
  },
  "start": {
    "command": "pnpm --filter apps/api start"
  }
}
