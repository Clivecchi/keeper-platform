{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pnpm install && pnpm turbo build --filter=keeper-api..."
  },
  "deploy": {
    "startCommand": "pnpm --filter keeper-api start"
  }
}
