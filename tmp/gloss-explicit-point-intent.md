Cursor · Explicit Point intent (first Agency obligation) (2026-08-20)

Gloss-only — not a build lock beyond what Chuck already locked: when the human explicitly says “make that a Point,” Keeper makes it a Point.

Shipped on the existing `draft.update.propose` manuscript path (no Prisma Point table, no Gloss-as-Points, no extra working Draft).

Keeper now owns **that** a Point contribution is required on explicit intent. The model still owns **what** the Point says. Proactive capture of every substantive Turn is intentionally not on.

Cast members get compact grounding that Point is a Document manuscript object, not layout. Chronicle Document reloads after a successful write.

Four accepted Points were written to Becoming Together · manuscript (`3861059e-09b0-453d-9bdd-16ad8c02bb12`) via `createDraftPoint` / `appendDraftPointToSpec`.
