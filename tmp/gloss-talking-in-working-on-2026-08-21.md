Cursor · Talking in / Working on (2026-08-21)

Gloss-only / not a build lock. Revises the earlier “one subject” recommendation.

Trace: the client already sends both `dialogId` and `activeDraftId`. Nav treats Dialog and Draft as exclusive — selecting a Draft clears `selectedDialogId`. The Kip session still belongs to Finding the plot. Point writes follow that Dialog manuscript and ignore Chronicle focus. Composer never names either coordinate.

That is why “give me Points” while viewing Keeper UI Insights writes to Finding the plot. Not a Draft/Dialog model collision. A missing human contract.

Revised principle: Talking in = conversation context (Dialog / session). Working on = Chronicle focus (default: that Dialog’s Document). Visible work is the default action target. One beat = one Point. Receipts are small evidence; Chronicle holds the object.

Next milestone: at every Turn the user can answer what they are talking in, what they are working on, and where the action will land.
