Cursor · Human Turn binds System One (2026-09-19)

Gloss-only / not a build lock unless Chuck already locked this on the Document.

Locked: System One orientation belongs to the Human Turn, not to lead_main or leftover prompt text.

Smallest implementation, no new Prisma model. Client mints a Human Turn id at send. Cast and Lead of that send share the id. Jev evaluates once on the Lead HTTP and is stored as a structured record on that Turn. Every later Lead generation in the same performance — lead_main and follow-ups — receives a rendering of that same record. Cast still does not see it, select models, or authorize actions.

Provenance on the Lead message now answers: which Human Turn, what Jev judged, which Cast voices, which Lead generations, which actions.

Desired shape remains: Human Turn → [System One + independent Cast] → Lead receives both. Confidence must not collapse Agency. Scene is not built yet; this is the Turn seam Scene will need.
