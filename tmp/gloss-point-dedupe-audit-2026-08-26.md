Cursor · Same Point twice · Audit is a record, not a Cast member (2026-08-26)

Gloss-only / not a build lock. From Ceox on Objective. Go Public: Chuck said yes to a Point; Ceox added it twice. Then the conversation named the deeper gap — the agent cannot audit itself.

**What shipped:** Keeper owns Point identity. Two `draft.update.propose` in one turn collapse to one. If that Point is already on the Document, Keeper does not append a second copy. The card says Already on this document, not Added twice. "Yes" after a Point offer is a Point, not Gloss, even if the prior turn mentioned the word gloss. Ceox was wrong that it cannot retire a Point — Review & Reorganize `change: retire` already exists. Points 15 and 16 on that Document are still there; retire one, do not wait for a new personality.

**Audit — recommendation, not a build:** Do not add an Audit-role agent. Audit is a job on evidence. A new voice would still lack timestamps and action receipts, and would still talk instead of checking. Point Types (`gap` / diagnostic) can label known failures so Review can ask "still true?" — they are not the audit. The real missing object is a **session action log**: what ran this session, with time, readable by the Lead. Receipts already exist as Dialog cards; the Lead cannot query them, so it reasons from the Document and invents "today." Self-audit is circular. Keeper should hold the record. The Lead reads it. The human Applies Document changes. Near-term, with what we have: Review & Reorganize plus a "Still True?" section, driven by receipts the platform can show — not a third Cast member.
