Cursor · Title Frame is slide one (2026-08-30)

Gloss-only. Not a build lock. Does not create Points or mutate the Document.

Chuck opened livecchi.biz as a guest. What loaded is already a Frame. The center card is already a Slide — `JourneyInvitationSlide`, SlideType `journey_invitation`. Forward already leaves that card for Present. The page is a one-slide story that does not admit it.

The leak: the top imprint is hardcoded `COVER_IMPRINT = "KE3P"` in `cover-frame.tsx`. It shows whenever the card title is not the string "KE3P". Personal domains therefore wear the platform brand above their own wordmark. The domain's `frame_json.theme.wordmark` is already `livecchi.biz`. Provision writes that. The card already uses it. The imprint ignores it.

Chuck's read: replace that KE3P with the actual domain, and let that slot become the title card. Then use this feeling to back into Stage composition — Keeper stories as a grouping of Frames / Slides, this one as slide #1. Also: it already moves us to some slide. Reign that in. Do not invent a second deck.

Recommended split (candidate lock, not built)
- **Title Frame** = the public Cover chrome. Persistent show title. Imprint is `theme.wordmark` (the domain), never hardcoded KE3P except on the platform domain itself.
- **Slide 1** = the card already there (`journey_invitation`): purpose + Forward. Not a new SlideType.
- **Forward** = next Frame in this domain's story (already aims at Present). Not a random door.
- **Stage** = member Shape of that same sequence. Frame 0 of a composed Stage story is this Title Frame — same family, not a third body.
- **Present** = guest/read (Show). Cover is already Show of a one-slide cut.

Do not do
- Do not print livecchi.biz twice (imprint + card) without a job for each. Imprint stays; card title can become the beat, or stay as wordmark while the imprint is the show name that survives Forward.
- Do not invent TitleSlide / TitleFrame as a new Prisma thing or a new SlideType this week.
- Do not put Dialog bubbles on Stage to make this work.
- Do not collapse Present into the Document. This page is Present/Cover, not Chronicle.

The one sentence
The guest Cover is already slide one of a Frame story. KE3P on that page is platform leakage. The domain wordmark is the title card. Stage composes more Frames of that same family. Present is how a guest reads them.
