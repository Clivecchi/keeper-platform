Cursor · Board rename, emphasis invariant, Glossary nav (2026-08-17)

Gloss-only — not a build lock. Lands the August 2026 close-out: Build board rename, governing board-emphasis invariant, in-product Glossary access.

**Build, not Strategy.** The former IDE board is labeled **Build** everywhere the five boards appear (Realm · Domain · Build · Design · Agent). Rationale holds: Draft focus, Points, Versions, Linked Dialog, Add to Document is tactical construction. Design defines; Build makes.

**`ide` is a routing key, not a label.** Internal id stays `ide` — kipMode, session resume, capability ceilings, TypeScript board ids. No database migration: workspace board is not a persisted user preference. Canonical URL write is `?board=build`; `?board=ide` still parses so bookmarks survive.

**Board-emphasis invariant** is now governing-tier in the Object Glossary (same standing as the EntityKind Recipe). Switching boards may change what's emphasized — never identity or available actions. Per-board lens table is in the glossary. Acceptance criterion for future board work.

**Standing exceptions, not silently patched:** Draft, Moment, Library, Domain-idle still violate via the Document-tree / Presence-tree fork (Section 2B). That remains a Rendr/product decision — this pass did not fold it in.

**Glossary in the UI.** Domain sidebar has a top-level Glossary entry (same tier as Dialogs, Keeper, Library, Journeys — not nested in Library). One click → Chronicle read of the governing markdown. Design board owns definition: Glossary in Design Nav opens Chronicle config layout. Not labeled as a Document anywhere in this UI.

Next logical task: Rendr conversation on the registry vs KeeperPresence fork so those four subjects can conform to the invariant.
