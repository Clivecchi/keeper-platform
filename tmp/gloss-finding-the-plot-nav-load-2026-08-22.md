Cursor · Finding the Plot is the Dialog (2026-08-22)

Gloss-only — not a build lock unless the Document already holds this.

Finding the Plot is the Dialog. The Document belongs to that Dialog. Re-Center, if it lives here, is a Section of that Document. Keeper UI Insights and Actions is another Section, carried by a Draft. Those are not the conversation.

The work inside Finding the Plot is Dialog, Document, Drafts, and Points coming together so a story can be made from the Dialog.

Nav load order is now: select → workspace (Header from Nav titles + conversation) → then Chronicle. Chronicle must not start the Document while the Header is still catching up.
