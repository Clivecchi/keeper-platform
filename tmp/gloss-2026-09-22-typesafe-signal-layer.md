Cursor · TypeSafe signal layer experiment (2026-09-22)

Gloss-only. Not a build lock. Not a live hook.

The no_stateful_obligation seam can be tested without putting scores on the turn. Current TypeSafe shadow runs before Lead, only when a reorganize phrase matches, and is delivered as a prose block. “Worth keeping” never called Jev. Conversation Profile already dropped the action sermon; it still lists every action name. Schemas stay in Zod until an action is parsed.

Smallest experiment: offline replay of Dialog Testing... testing.. 1, 2, 3... (cmuc18qo00001nt01cyx3lm1n). Two frozen packets per turn — human plus compact Keeper state, then the same packet plus the stored reply. Five fixed questions: Initiative, Obligation, Responsibility, Likely capability, Orientation Alignment. Blank Orientation on a young Dialog is an acceptable alignment. Three repeats. Log the packet, raw answers, tokens, latency, keyword result, and what Keeper actually did (nothing new).

Scores do not authorize, do not rewrite Orientation, and do not re-enter the model. If the same packet straddles a threshold, it cannot drive an obligation. Initiative may belong before the reply; Obligation and Orientation Alignment belong after it. A single post-response pipe is the hypothesis under test, not the architecture.
