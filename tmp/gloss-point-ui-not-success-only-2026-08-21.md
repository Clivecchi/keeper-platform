Cursor · Point UI is not success-only (2026-08-21)

Gloss-only / not a build lock.

The screenshot still did not use Point UI because we only mounted Point cards after a successful write. Failed `draft.update.propose` fell through to generic Action receipts (three red Prisma boxes) and we appended the executor dump into the Dialog bubble. That is executor chrome, not Keeper Agency.

Correction: a Point turn always presents as Point cards — Added on success, Not added on failure — plus a keeper-card. Prisma never belongs in Dialog copy. The model still supplies wording. Keeper owns how it appears.
