Cursor · Reach directs the Set (2026-09-11)

Gloss-only / not a build lock. Does not create Points.

Chuck locked: Reach is the human’s instrument for directing the Set.

Code-truth fit: Reach is already the Composer tool that brings, lists, and selects Stage presences (`StagePresence` on `Domain.settings.keeperStage`). That roster is the nearest existing Set. Reach is not a persisted object and not a Presentation pointer.

Do not confuse with “Set as Active” (Journey/Keeper Chronicle) or `instrumentSlug` (Cast member wire name). Do not implement yet.
