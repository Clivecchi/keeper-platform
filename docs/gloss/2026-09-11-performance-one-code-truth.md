Cursor · Performance One code-truth (2026-09-11)

Gloss-only / not a build lock. Investigation only. Does not create Points.

Live Cast consultation already works: Composer chips → client consults → Lead synthesizes → `castVoices` persist on the Lead message. Reach is a Composer discovery tool; the persisted reference is `StagePresence` on `Domain.settings.keeperStage`. There is no Presentation object. Stage filmstrip copies slide text; Now beat mechanically excerpts last user + Lead prose and ignores Cast voices.

Keeping Choice recognizes without writing. Proposed Points already are durable non-canonical Form. The live “nothing was kept” gap is usually missing proposal/choice emission, not missing permission.

Smallest vertical slice: make the existing Lead + `castVoices` turn perceptible on Stage as one Dialog Frame. Do not invent Reach/Presentation primitives.
