Cursor · Keeping Choice exercise prompt shipped; Casting replay blocked (2026-09-11)

Gloss-only / not a build lock.

Shipped on `cloud` `d1b330b5`: rewrite of `formatKeepingChoiceLeadInput` and `buildKeepingChoiceExercisePrompt`. Contract tests passed (9). Railway production is on that commit. Optional `sole.save` detector was not shipped.

Replay of Keep the Casting principle was not completed. Original choice `dc7ea222` is already selected. Vercel web bundle still contains “keep it appropriately.” Browser session was unsigned. No model keys locally. Did not invent a backstop or correct after the fact.

Proof of Agency judgment still waits on a signed-in Becoming Together exercise turn after the web bundle picks up the new lead input — or a new offer of the same meaning, then select.
