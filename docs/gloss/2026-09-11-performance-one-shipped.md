Cursor · Performance One shipped (2026-09-11)

Gloss-only. Does not create Points.

Approved lock implemented: `resolvedMeaning` (`meaning`, not `claim`) is a Lead `agent_output` sibling. Stage + Cast synthesis asks for it. Persist on the Lead message. If missing, Rendr is skipped and Stage is unchanged.

Post-Lead ephemeral Rendr receives only Resolved Meaning + compact Set/coordinates. One `stage_expression` beat. Keeper appends one `text_slide` with `source: { kind: 'live', id: leadMessageId }`. Does not replace Cover or rewrite the story. `stage.story.layout` stays Lead-only.

Timeout or parse miss: Dialog stands, Stage does not pretend a Frame occurred.

Composer directing-act capture remains a future seam.
