Cursor · Cast honesty — Kip never asked (2026-08-22)

Gloss-only. Not a build lock. Does not create Points or mutate the Document.

Chuck’s 2:48–2:50 Dialog on ke3p is the Agent problem, not the Agent cover problem.

Asked: each agent asks one question about the document. Observed: document not in Chronicle focus; Ceox and Rendr asked; Kip never asked; Kip then wrote ### Cloud / ### Rendr in his own bubble; Cloud said no consult came through; Rendr inferred the document from the thread; second turn was process commentary, still no Kip question.

Three stacked failures, already named in code as rules the model may ignore:

1. Puppet show. Lead must not invent another agent’s words unless Mechanism A (cued Cast voice cards) or Mechanism B (delegate.consult) actually returned. Prompted in DOMAIN AGENTS, keeper-card rendering, and synthesis. Kip still performed Cloud and Rendr. Cloud’s “no consult came through” is the honest signal.

2. Lead is encoded as synthesizer, not peer. Cast consults exclude the director. Delegation prompts say Kip will synthesize. So “each agent ask a question” never includes Kip asking. Rendr’s excuse (“Lead synthesizes, not interrogates”) is reading Keeper’s own framing.

3. Document identity is not locked. dialog.read / DIALOG DOCUMENT inject when a Dialog manuscript is present. Chronicle focus is a different signal. Ambient thread lets Rendr and Kip talk about “the document” when Chuck can see it is not in focus. Cloud refused to guess. That refusal should be required, not optional virtue.

Recommended first move: enforce Cast honesty (strip/reject ### Cloud / ### Rendr in Lead prose when no real consult exists). Then lock one product question: when the user asks every agent to do X, must Kip do X in his own voice, or only synthesize? Then hard-line “no Document in Chronicle focus — do not guess.”

Do not start with Agent EntityKind cover completeness. Do not implement Persona. Do not add a fourth panel.
