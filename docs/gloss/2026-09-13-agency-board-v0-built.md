Cursor · Agency Board V0 built (2026-09-13)

Gloss-only. Not a further build lock. Does not create Points or mutate the Document.

Locked design is now in code. Agency Place is idle Chronicle on Agency Board. Finding the Plot is Now when ke3p's last domain-scoped Lead performance is that Dialog. Present is Keeper's working Agency; Lens, Contract, and provenance sit in inspection, not on the path title. Needs you and Becoming stay empty.

What the implementation revealed

- Runtime Board id is still `agent`. `?board=agency` is the written URL. `?board=agent` still opens the same board. Conversation remains Agent Studio (`monologue` + Echo). That leftover is visible once you talk; it is not this slice.
- Trail's idle crumb is still the Domain name. Chrome says Agency. Returning from Kip or Finding the Plot lands on Agency Place because idle Chronicle is board-aware, not because Trail learned a new kind.
- People Nav reuses `GET /members` (admin). Agency Place owner/lead facts use read permission. A non-admin can see Place and fail the People list. That is existing Domain People authority, not new storage.
- Now is judged, not hardcoded. On ke3p today it is Finding the Plot. If the last named Lead performance changes, Now changes. If it cannot be named, Now is absent.
- Inspection can show recorded layers from that performance. It does not put CeoX on Cover. Cast mismatch remains a discovered tension, not V0 terrain.
- The Agents Cover reach does not open a warehouse. It returns to Place. Agents live in Nav.

Do not fill empty terrain to look complete.
