# Root Pages

## 📌 Purpose
Legacy root routes retained for compatibility and redirected to modern settings surfaces.

## 🧱 Key Files
- `RootDashboardPage.tsx`

## 🔄 Data & Behavior
Root routes now redirect to `/settings` to avoid legacy UI fragmentation.

## ⚠️ Notes & ToDo
- [ ] Remove root routes entirely once legacy links are retired.

## 📆 Update Log
- 2026-01-25: Converted the root dashboard route to redirect into `/settings`.
