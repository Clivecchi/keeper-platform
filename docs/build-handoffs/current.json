{
  "version": "1.0",
  "id": "realm-header-cast-and-multiselect",
  "created_at": "2026-07-21T00:00:00Z",
  "created_by": "cloud",
  "goal": "Rework director-mode agent presentation universally: cast/roster identity displayed in the header, invocation happens at the composer, the lead agent is invoked by default (always-on, not a click), and Domain/Realm specifically supports true multi-select of non-lead instruments for collaboration -- unlike IDE/Designer's single-active-instrument swap.",
  "done_when": [
    "A header slot (universal -- applies to every board using dialogOrchestration: 'director', not just Domain/Realm) displays the cast/roster: who is lead, which instruments are available. This is identity display, not necessarily the click target itself.",
    "The actual invoke/select interaction for instruments lives at or near the composer, not only in the header -- Cursor decides and documents the concrete split (e.g. header shows identity, composer shows a compact active/selectable row; or header chips are themselves clickable and composer reflects state) as long as 'cast managed in header, invoked at composer' is genuinely true afterward, not just relabeled",
    "The lead agent (directorAgentSlug) is invoked/active by default on load -- no click required to engage Kip (or Rendr on Designer) as the default responder. This is a real behavior change from today, where activeBoardInstrumentSlug starts null/disengaged.",
    "On Domain/Realm specifically: true multi-select -- multiple non-lead instruments (e.g. Cloud AND Rendr) can be simultaneously active for collaboration, not one-at-a-time swap. This requires changing the underlying active-instrument model from a single activeSlug to a set for this board only -- confirmed by code review that BoardInstrumentsBar, UniversalConversation's activeBoardInstrument state, and the onInvoke wiring are all single-slug today, with no multi-select mechanism anywhere in the codebase",
    "IDE and Designer explicitly keep today's single-active-instrument swap behavior -- this handoff does not change their interaction model, only Domain/Realm's",
    "Whatever agent is actually invoked for a given message (lead alone, or lead + one or more collaborating instruments) is visually clear in the Dialog stream itself, not just in the header/composer control -- Cursor's call on exact presentation, document it",
    "pnpm run quick:web passes",
    "Manual check on /d/ke3p?board=realm: Kip is active by default without clicking anything; Cloud and Rendr can both be turned on simultaneously alongside Kip; cast identity reads from the header; the actual invoke control is at the composer. Manual check on /d/ke3p?board=ide: unchanged single-swap behavior still works exactly as before."
  ],
  "territory": "cursor",
  "canon": ["AGENTS.md", "docs/universal-board-dialog-orchestration.md", "docs/chronicle-document-architecture.md"],
  "scope": {
    "touch": [
      "apps/web/src/v0/boards/components/BoardInstrumentsBar.tsx",
      "apps/web/src/v0/components/dialog/KeeperDialogFrame.tsx",
      "apps/web/src/v0/boards/UniversalConversation.tsx",
      "apps/web/src/v0/realm/DialogCastBar.tsx (if a header identity component is still needed, or its remaining trailing-actions role changes)",
      "apps/web/src/v0/boards/UniversalBoardDefinition.ts (only if board-def fields need a new flag to express Domain/Realm's multi-select vs IDE/Designer's single-select -- Cursor's call, document it)"
    ],
    "do_not_touch": [
      "packages/database/prisma/schema.prisma",
      "apps/api/",
      "the full future 'director' delegation model (Kip receiving a message and internally delegating to Cloud/Rendr as sub-turns with action cards) -- this handoff is about the invocation UI and which instruments can be simultaneously engaged, not about building that delegation pipeline",
      "the Library/Kept-Presented curation question (Known Issue, already parked separately) -- unrelated to this handoff"
    ]
  },
  "pattern_ref": "Chuck's own framing, verbatim, is the spec: 'I want the cast to be managed through the header. But invokeable at the composer.' And: 'Kip is the lead agent and should be invoked by default and shown as a chip. The other agents can be selected as needed. The minor difference than what exists on IDE is that multiple cast agents can be selected for collaboration versus just one.' Both requirements are explicit, not inferred. Verify BoardInstrumentsBar's activeSlug/onInvoke shape and UniversalConversation's activeBoardInstrument state before assuming how much needs to change -- confirmed single-slug throughout as of realm-director-mode-unification (archived).",
  "rendr_treatment": null,
  "git": {
    "branch": "cloud",
    "base": "cloud",
    "commit": true,
    "open_pr": false,
    "pr_title": "feat: header cast identity + composer invocation, Kip default-active, Realm multi-select instruments"
  },
  "verification": {
    "commands": ["pnpm run quick:web"],
    "browser": ["/d/ke3p?board=realm", "/d/ke3p?board=ide"]
  },
  "context": {
    "phase": "Follow-up to realm-director-mode-unification (archived, shipped) -- Chuck reviewed the unified result and named three real requirements the unification didn't cover: header-based cast display, default-active lead, and true multi-select for Realm's collaboration model, distinct from IDE's single-swap.",
    "related_handoffs": [
      "realm-director-mode-unification (archived, shipped -- shared BoardInstrumentsBar across boards, but single-activeSlug model throughout, cast bar demoted to trailing chrome only)",
      "ke3p-becoming-together-consolidation (archived, shipped -- unrelated to this handoff)"
    ],
    "library_refs": [],
    "notes": "Two clarifications from the same conversation, recorded so they aren't relitigated: (1) Library items are intentionally domain-wide, not Dialog-scoped -- that's the platform's real, documented architecture (Domain -> LibraryItem, not hierarchical), not a bug. Cloud had wrongly framed this as a gap in an earlier report; corrected. (2) The 47 Kept + 8 Presented items visible in Realm's Nav are real, pre-existing Library items and kept Moments -- separate from and unrelated to the 78 archived drafts from ke3p-becoming-together-consolidation, which are already correctly excluded from the default view. The flat, undifferentiated feel of that list is the long-parked Curator/grouping work, not something this handoff or the consolidation was ever meant to solve -- explicitly out of scope here, not silently bundled in."
  }
}
