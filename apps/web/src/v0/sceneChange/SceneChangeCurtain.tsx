/**
 * @deprecated Use DomainLoadCurtain — kept for import stability.
 */
export {
  DomainLoadCurtain as SceneChangeCurtain,
  type DomainLoadCurtainProps as SceneChangeCurtainProps,
} from "./DomainLoadCurtain"

export {
  DOMAIN_SHELL_MIN_HOLD_MS as SCENE_MIN_HOLD_MS,
  DOMAIN_SHELL_WARM_SKIP_MS as SCENE_WARM_SKIP_MS,
} from "../boards/domain/domainShellBootstrap"
