/**
 * Presence layout modes — drive real rendering behavior in KeeperPresence.
 *
 * focus  — the object as story; default Chronicle state
 * config — the object being shaped; shifted surface, still board-connected
 */
export type PresenceLayout = "focus" | "config"
