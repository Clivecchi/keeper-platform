/** Runtime slug registered by V0Shell / board theme hook for domain + hierarchy resolution. */
export const DOMAIN_THEME_SLUG = 'domain-resolved'

/** Default bundled theme when domain JSON has no brand colors (gray earth tone). */
export const DEFAULT_BASE_THEME_SLUG = 'gray-earth'
