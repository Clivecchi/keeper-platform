/**
 * Warm V0Shell data for a domain before navigation (hover on domain switcher).
 * @deprecated Import from `domainShellCache` — re-export kept for existing imports.
 */

export { prefetchDomainShell } from "./domainShellCache"
