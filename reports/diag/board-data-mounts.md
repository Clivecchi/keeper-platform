Board-Data Mounts
mountedBase: /api/board-data
routers mounted: kamOrUserAuth ← ./middleware/auth-combined.js, kamOrUserAuth ← ./middleware/auth-combined.js, newBoardRoutesDefault ← ./api/boards.js, boardStudioAliasRouter ← ./api/boards.js
default export var: router
routes on default: GET /, POST /, GET /:id, GET /:id/raw, POST /:id/frames, POST /:id/frames-data, PATCH /:id/frames-data/:frameId, DELETE /:id/frames-data/:frameId, PATCH /:id/frames-data/reorder, PATCH /:id/frames/:frameId, DELETE /:id/frames/:frameId, PATCH /:id/frames/reorder
foundGetIdHandler: true
NOTE: PARTIAL (scanned TS fallback, compiled JS not found)