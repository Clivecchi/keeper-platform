[build]
builder = "nixpacks"
buildCommand = "pnpm install && pnpm run build && ls -la dist/"

[deploy]
startCommand = "pnpm start"
healthcheckPath = "/api/test"
healthcheckTimeout = 100
restartPolicy = "on-failure"
restartPolicyMaxRetries = 10

[env]
NODE_ENV = "production" 