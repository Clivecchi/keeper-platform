export {};
/**
 * Type definitions for client-side hooks.
 * Used to strongly type hook returns (e.g., session shape, status enum).
 * Should import shared types from /kam/types where appropriate.
 */ 
